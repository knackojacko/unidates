<% if @user %>
    <% if @user.errors.any? %>
          <div id="error_explanation" class="col-md-12">
            <div class="alert alert-danger" role="alert">
              Oh shucks! Please fix <%= pluralize(@user.errors.count, "error") %> associated with your information:
            </div>
            <table class="table table-striped">
              <% @user.errors.each do |attr, msg| %>
                  <tr><td><%= msg %></td></tr>
              <% end %>
            </table>
          </div>
    <% end %>
<% end %>